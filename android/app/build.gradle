plugins {
    id 'com.android.application'
}

android {
    namespace 'oblivion'
    compileSdk 36
    ndkVersion '27.2.12479018'

    defaultConfig {
        applicationId "org.TheJiggleDev.Oblivion"
        minSdk 31
        targetSdk 36
        versionCode 1
        versionName "1.0"
        ndk{
          abiFilters 'arm64-v8a' 
        }
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
        debug{

        }
    }

    externalNativeBuild {
        cmake {
            version '4.4.0+'
            path '../CMakeLists.txt'
        }
    }

    // Enable prefab support for the OpenXR AAR
    buildFeatures {
        prefab true
        buildConfig = true
    }

}

dependencies {
    implementation 'org.khronos.openxr:openxr_loader_for_android:1.1.49'
}
